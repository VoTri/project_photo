<div class= "photo">
<h1><%= t("views.photos.new.new_photo") %></h1>

<%= render 'form', photo: @photo, class: "btn btn-danger" %>

<%= link_to t("views.photos.index.back"), photos_path, class: "btn btn-danger" %>
</div>
